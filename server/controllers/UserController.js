const Trainee = require("../models/Trainee");
const Instructor = require("../models/Instructor");

async function User(req, res) {
  const id = req.headers.id;
  // if(req.headers.type === "Trainee"){  to do
  Trainee.mong;

  //}
  await trainee.find;
  res.send("User country has been added!");
}
module.exports = User;
